# 更新记录

记录每次修改的内容与解决的问题，遵循：极简、通俗易懂、不过度设计。

## 2026-09-05

### 5. 月份选择汉化 + 账单按日折叠
- 顶部月份选择器显示格式改为中文「YYYY年M月」，`dayjs` 全局设为 `zh-cn`（配合 antd zhCN），日历面板/星期均为中文
- 交易列表改为按日期分组：同一日期的多笔记录合并为一个可折叠分组，默认全部展开
- 分组标题显示「M月D日 + 周几 + 笔数 + 当日支出/收入合计」，组内行省略日期只显示分类/备注/金额与操作
- 组内行与组间使用细分割线区分

### 4. 补充 README 启动说明
- 根目录 README.md 补充前后端启动方法：数据库准备、环境变量配置、后端 uvicorn 启动、前端 npm run dev
- 附带常用命令表与目录结构说明

### 1. 前端整体风格改为清爽浅色记账风
- 清理 Vite 脚手架残留的默认样式（index.css / App.css 重写）
- 引入 antd `ConfigProvider` 主题：近黑主色、细浅边框、柔和圆角、暖米白背景 `#f6f4ef`、中文 locale
- 布局重排：居中限宽 680px、标题区、统计卡加大加粗（收入绿/支出红/结余黑）、通栏主按钮「+ 记一笔」
- 交易列表重排：分类 + 日期·备注、正负彩色金额、删除加二次确认
- 金额输入改 `InputNumber`；月份切换改 antd 月份选择器；页面标题改「记账本」

### 2. 记账分类固定为可选列表
- 「记一笔」弹窗的分类由自由文本框改为下拉固定选项
- 新增 `CATEGORIES` 常量：收入（工资/奖金/理财/兼职/退款/其他）、支出（餐饮/交通/购物/居住/水电/娱乐/医疗/人情/其他）
- 分类选项随类型切换，切换类型时自动清空已选分类
- 表单加默认值：类型默认「支出」、日期默认今天

### 3. 已记录账单支持编辑
- 列表每行新增「编辑」入口，与「删除」并列
- 点击编辑复用同一弹窗，预填该条数据；弹窗标题区分「记一笔 / 修改记录」
- 提交时按是否编辑调用 `updateTransaction`（PUT）或 `createTransaction`（POST），成功后刷新统计与列表
- 编辑历史数据时，若其分类不在固定选项内会自动补入下拉，避免丢值

> 备注：前端 `npm run lint`、`npm run build` 均通过。
